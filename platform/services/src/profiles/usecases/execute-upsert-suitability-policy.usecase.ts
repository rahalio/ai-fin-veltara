/**
 * ExecuteUpsertSuitabilityPolicy - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: profiles
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteUpsertSuitabilityPolicy Use Case
 *
 * DDD: Application use case for get SuitabilityPolicy.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { UpsertSuitabilityPolicyInput, UpsertSuitabilityPolicyOutput } from "../dto/suitability-policy.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veltara/services/_shared/index.js";
import type { SuitabilityPolicyRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteUpsertSuitabilityPolicy {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly suitabilityPolicy: SuitabilityPolicyRepository
  ) {}

  async execute(input: UpsertSuitabilityPolicyInput): Promise<UpsertSuitabilityPolicyOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.prfId();

    // All field names extracted from OpenAPI spec: maxRiskScore, name, requireHumanGate, rulesSummary, status, version
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).policyId) {
      throw new ValidationError("policyId is required");
    }
    const policyId = (input as any).policyId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.suitabilityPolicy.upsertSuitabilityPolicy({ policyId: policyId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${policyId}`);
    }

    return entity as UpsertSuitabilityPolicyOutput;
  }
}
