/**
 * ExecuteGetRecommendation - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: recommendations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetRecommendation Use Case
 *
 * DDD: Application use case for get Recommendation.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetRecommendationInput, GetRecommendationOutput } from "../dto/recommendation.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veltara/services/_shared/index.js";
import type { RecommendationRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetRecommendation {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly recommendation: RecommendationRepository
  ) {}

  async execute(input: GetRecommendationInput): Promise<GetRecommendationOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.recId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).recommendationId) {
      throw new ValidationError("recommendationId is required");
    }
    const recommendationId = (input as any).recommendationId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.recommendation.getRecommendation({ recommendationId: recommendationId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${recommendationId}`);
    }

    return entity as GetRecommendationOutput;
  }
}
