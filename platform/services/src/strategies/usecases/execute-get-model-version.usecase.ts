/**
 * ExecuteGetModelVersion - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: strategies
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetModelVersion Use Case
 *
 * DDD: Application use case for get ModelVersion.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetModelVersionInput, GetModelVersionOutput } from "../dto/model-version.dto";
import type { ExecutionContextService, IdGeneratorService } from "@veltara/services/_shared/index.js";
import type { ModelVersionRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetModelVersion {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly modelVersion: ModelVersionRepository
  ) {}

  async execute(input: GetModelVersionInput): Promise<GetModelVersionOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.stgId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).modelVersionId) {
      throw new ValidationError("modelVersionId is required");
    }
    const modelversionId = (input as any).modelVersionId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.modelVersion.getModelVersion({ modelVersionId: modelversionId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${modelversionId}`);
    }

    return entity as GetModelVersionOutput;
  }
}
