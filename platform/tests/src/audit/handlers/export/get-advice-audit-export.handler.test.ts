/**
 * GetAdviceAuditExport handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: audit
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getAdviceAuditExport } from "@veltara/api-server/domains/audit/handlers";
import type { AuditDDDDependencies } from "@veltara/api-server/domains/audit";
import { createExport as createExportEntity } from "@veltara/factories/audit";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@veltara/tests/shared";
import { AUDIT_USE_CASES } from "../__factories__/use-cases.js";

describe("GetAdviceAuditExport", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: AuditDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<AuditDDDDependencies>(
      AUDIT_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createExportEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetAdviceAuditExport, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { exportId: entity.id };

    await getAdviceAuditExport(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetAdviceAuditExport.execute).toHaveBeenCalledWith(expect.objectContaining({ exportId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { exportId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetAdviceAuditExport, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getAdviceAuditExport(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
