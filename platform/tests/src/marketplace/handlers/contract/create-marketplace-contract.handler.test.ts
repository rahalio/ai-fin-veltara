/**
 * CreateMarketplaceContract handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: marketplace
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { createMarketplaceContract } from "@veltara/api-server/domains/marketplace/handlers";
import type { MarketplaceDDDDependencies } from "@veltara/api-server/domains/marketplace";
import { createContract as createContractEntity } from "@veltara/factories/marketplace";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@veltara/tests/shared";
import { MARKETPLACE_USE_CASES } from "../__factories__/use-cases.js";

describe("CreateMarketplaceContract", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: MarketplaceDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<MarketplaceDDDDependencies>(
      MARKETPLACE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 201 with created entity", async () => {
    const input = {} as any;
    const entity = createContractEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeCreateMarketplaceContract, "execute").mockResolvedValue(useCaseResult);

    await createMarketplaceContract(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeCreateMarketplaceContract.execute).toHaveBeenCalledWith(expect.anything());
    expect(mockReply.code).toHaveBeenCalledWith(201);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
