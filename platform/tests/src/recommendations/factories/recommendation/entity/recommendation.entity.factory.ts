/**
 * Recommendation Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: recommendations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import type { RecommendationEntity } from "@veltara/core/recommendations/models/index.js";
import { recommendationsSchemas } from "@veltara/core/recommendations";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "rec";

export function createRecommendation(overrides: Partial<RecommendationEntity> = {}) {
  const raw = {
    id: overrides.id ?? `rec_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    clientId: overrides.clientId ?? `cli_${ulid().toLowerCase()}`,
    status: overrides.status ?? "pending_gate",
    strategyId: overrides.strategyId ?? `str_${ulid().toLowerCase()}`,
    ...overrides,
  };

  return recommendationsSchemas.Recommendation.parse(raw);
}

export function recommendationBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createRecommendation(overrides));
}
