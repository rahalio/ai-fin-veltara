/**
 * UpsertRecommendationRiskParameters handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: recommendations
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { upsertRecommendationRiskParameters } from "@veltara/api-server/domains/recommendations/handlers";
import type { RecommendationsDDDDependencies } from "@veltara/api-server/domains/recommendations";
import { createRiskParameter as createRiskParameterEntity } from "@veltara/factories/recommendations";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@veltara/tests/shared";
import { RECOMMENDATIONS_USE_CASES } from "../__factories__/use-cases.js";

describe("UpsertRecommendationRiskParameters", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: RecommendationsDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<RecommendationsDDDDependencies>(
      RECOMMENDATIONS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createRiskParameterEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { recommendationId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpsertRecommendationRiskParameters, "execute").mockResolvedValue(useCaseResult);

    await upsertRecommendationRiskParameters(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpsertRecommendationRiskParameters.execute).toHaveBeenCalledWith(expect.objectContaining({ recommendationId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
