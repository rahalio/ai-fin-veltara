/**
 * Strategy Entity Factory
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: strategies
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { faker } from "@faker-js/faker";
import type { StrategyEntity } from "@veltara/core/strategies/models/index.js";
import { strategiesSchemas } from "@veltara/core/strategies";
import { ulid } from "ulid";

// Domain prefix from OpenAPI spec info.x-domain
const DOMAIN_PREFIX = "stg";

export function createStrategy(overrides: Partial<StrategyEntity> = {}) {
  const raw = {
    id: overrides.id ?? `str_${ulid().toLowerCase()}`,
    createdAt: overrides.createdAt ?? new Date().toISOString(),
    updatedAt: overrides.updatedAt ?? new Date().toISOString(),
    evidenceStatus: overrides.evidenceStatus ?? "unverified",
    name: overrides.name ?? faker.person.fullName(),
    provenance: overrides.provenance ?? "ai",
    status: overrides.status ?? "draft",
    ...overrides,
  };

  return strategiesSchemas.StrategyPackage.parse(raw);
}

export function strategyBatch(n = 3, overrides = {}) {
  return Array.from({ length: n }, () => createStrategy(overrides));
}
